# Interaction Library

Recently wrapped glass effect shader with Three js and GLSL, implemented FOB(frame buffer). That was particularly needed inorder to capture the effect from previous frame.

## Upcoming

Zenstone -> Implemented device orientation api(has to clear some issues), plan is to paint using phone movements.

I was trying to add just an image and along with it, the sounds associated ~ this is the thought, lets seeeee

- Right now I have added just the music, associating it with the object. I wonder how it can have it's variation throughout the object